# include <iostream>
using namespace std;
int main(){
    int i;
    //If else is used for the conditional statements that have to outcomes on two diff conditions
    cout<<"the value of the number"<<endl;
    cin>>i;
    if(i<10){
        cout<<"the value of the number is less than zero";
        }
        else{
            cout<<"the value of the number is greater than zero";
        }
    
    return 0;
}

// the if else statement are used for diff code structures example loop and selection structure where there are different conditions